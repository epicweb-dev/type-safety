# Exact Values

👨‍💼 Let's use literal types to constrain values to specific options.

🐨 Open <InlineFile file="index.ts" /> and:

1. Create literal types for allowed values
2. Use them to type-check function parameters
3. See how TypeScript catches invalid values

💰 Literal type union:

```ts
type Size = 'small' | 'medium' | 'large'
```

📜 [TypeScript Handbook - Literal Types](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#literal-types)

# Single Source of Truth

👨‍💼 Perfect. The routes object now powers both runtime behavior and compile-time
types. No enums, no duplication.

When the object changes, `RouteName` and `RoutePath` update automatically.

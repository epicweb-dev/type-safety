# Preserve Literals

👨‍💼 Nice! The role config now preserves literal booleans, so the type system
can distinguish `true` from `false`.

The `Role` type comes straight from the data, which means adding or removing a
role automatically updates the type.

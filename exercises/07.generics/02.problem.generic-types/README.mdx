# Generic Types

👨‍💼 Generics aren't just for functions—you can create generic **types** and
**interfaces** too!

```ts
// Generic type alias
type Container<T> = {
	value: T
}

// Generic interface
interface Box<T> {
	contents: T
}

// Usage
const stringBox: Box<string> = { contents: 'hello' }
const numberBox: Box<number> = { contents: 42 }
```

This is exactly how built-in types like `Array<T>` and `Promise<T>` work.

🐨 Open <InlineFile file="index.ts" /> and:

1. Create a generic `Result<T>` type for success/error handling
2. Create a generic `Pair<T, U>` type with two different types
3. Create a generic `ApiResponse<T>` interface for API responses
4. Use these types in functions

💰 Result pattern:

```ts
type Result<T> = { success: true; data: T } | { success: false; error: string }
```

📜 [TypeScript Handbook - Generic Types](https://www.typescriptlang.org/docs/handbook/2/generics.html#generic-types)

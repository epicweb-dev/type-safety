# Narrowing

<EpicVideo url="https://www.epicweb.dev/workshops/type-safety/2-union-types/narrowing/solution" />

👨‍💼 Great narrowing! TypeScript follows your checks and knows the specific type
in each branch.

🦉 Checking for unique properties is a reliable way to narrow union types because:

- TypeScript narrows the entire object
- You can write simple, readable branching logic

<callout-info>
	We’ll cover discriminated unions (the `type` property pattern) in the next
	exercise.
</callout-info>

# Literal Types

👨‍💼 You've mastered literal types!

You learned:

- 📌 **Exact values** as types, not just general types
- 🎲 **Number literals** for constrained numeric values
- 🚦 **State machines** with literal union types
- 🔒 **`as const`** to preserve literal types

🦉 Literal types are the foundation of "making illegal states
unrepresentable." Instead of accepting any string and validating at runtime,
you only accept valid strings at compile time.

```ts
// ❌ Runtime validation needed
function setDirection(dir: string) {
	if (!['north', 'south', 'east', 'west'].includes(dir)) {
		throw new Error('Invalid direction')
	}
}

// ✅ Compile-time safety
function setDirection(dir: 'north' | 'south' | 'east' | 'west') {
	// Invalid directions are impossible!
}
```

Next up: Intersection types—combining multiple types!

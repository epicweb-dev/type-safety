# Object Shapes

👨‍💼 Let's create interfaces for our domain objects. Interfaces are the standard
way to define object shapes in TypeScript.

🐨 Open <InlineFile file="index.ts" /> and:

1. Create a `User` interface
2. Create a `Product` interface
3. Create functions that accept these interfaces

💰 Interface syntax:

```ts
interface Name {
	property: Type
}
```

📜 [TypeScript Handbook - Interfaces](https://www.typescriptlang.org/docs/handbook/2/objects.html)

🐨 Once you've completed the exercise, run `node index.ts` in the playground to test your work!

# Object Shapes

👨‍💼 Clean interface definitions! Notice how the interface makes the object
shape crystal clear.

🦉 The optional `description?` property is handled elegantly—you can include it
or not, and TypeScript tracks which objects have it.
